#pragma once

#include <functional>

#include "MainStruct.hpp"
#include "World.hpp"

namespace Heerbann {

	class ShaderProgram;

	namespace AI {
		





		class AIHandler {

		};













		/*
		struct Debug {
			float radius;
			float position[2];
		};
		
		struct Entitiy {
			unsigned int locked = 0;
			bool isActive = false;
			unsigned int id;
			unsigned int index;

			float position[2];
			float dir[2];
			float speed;
		};

		struct MapEntry {
			int count;
			Entitiy entries[100];
		};

		class AIHandler {

			ShaderProgram* shader, *compShader;

			GLuint vao, vbo;
			GLuint ssbo_debug, ssbo_spatial, ssbo_entity;

			GLuint transLoc;

		public:
			void create();
			void draw(const sf::Transform&);
		};
		*/
		
	}
}